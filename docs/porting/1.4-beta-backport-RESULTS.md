# 1.4 "Cohesive Horizons" worldgen backport (Stage 1) — RESULTS / morning report for Maintainer

**Run:** overnight 2026-06-06, autonomous (Opus, high reasoning), per `docs/porting/HANDOFF-1.4-beta-backport.md`.
**Outcome: ALL THREE VERSIONS LANDED + PROVEN.** 1.21.11, 1.21.1, 1.20.1 each compile, build a versioned jar, and pass the §5 map-based atlas targets on both proof seeds. No guardrail violations.

> This file is written to the working tree of the canonical repo (currently on `feat/1.3.1-cohesive-horizons-26.1.2`) as an **untracked** report. It was deliberately NOT committed to that branch (guardrail). Move/commit it wherever you like.

---
## TL;DR table

| MC | Status | Branch (pushed) | Jar | SHA-256 | Local save tag |
|---|---|---|---|---|---|
| **1.21.11** | **LANDED** | `port/1.4.0-beta-1.21.11` | `latitude-1.4.0-beta.1+1.21.11.jar` | `f408d5f6dd021065bfd2b868abb20cfe93464ccdecaba0c2aee11053b4dd14da` | `save/1.4.0-beta-worldgen-1.21.11` |
| **1.21.1** | **LANDED** | `port/1.4.0-beta-1.21.1` | `latitude-1.4.0-beta.1+1.21.1.jar` | `333d8fcfeeee6b3616596df9bd2caecf624fd829743e19b56ca96ce65b860373` | `save/1.4.0-beta-worldgen-1.21.1` |
| **1.20.1** | **LANDED** | `port/1.4.0-beta-1.20.1` | `latitude-1.4.0-beta.1+1.20.1.jar` | `341c6f7365cbbe4d7a982f886d77f0ae25182fb52faa02789da9cce697218c22` | `save/1.4.0-beta-worldgen-1.20.1` |

All three branches are **pushed** to `origin`. **No tags were pushed** (the `save/*` tags are local-only, as required). Guarded baselines verified unchanged: `origin/main` = `4ccb9e9e…`, `origin/feat/1.3.1-cohesive-horizons-26.1.2` = `691e54b4…`.

**Jar paths (local worktrees):**
- `<home>/CascadeProjects/Latitude-port-1.4.0-1.21.11/build/libs/latitude-1.4.0-beta.1+1.21.11.jar`
- `<home>/CascadeProjects/Latitude-port-1.4.0-1.21.1/build/libs/latitude-1.4.0-beta.1+1.21.1.jar`
- `<home>/CascadeProjects/Latitude-port-1.4.0-1.20.1/build/libs/latitude-1.4.0-beta.1+1.20.1.jar`

---
## What landed (the same Stage 1 / Bucket 1 transplant on all three)

Algorithm transplant (not a cherry-pick — 26.1.2 is Mojang mappings; targets are Yarn) of the 1.4 worldgen pass into each target's existing 1.3.0 `LatitudeBiomes.java`:
- **Art VI `pickFrom` coherent noise** (`da1bc0dd`) — LANDED verbatim. Argmax over N coherent `ValueNoise2D` fields (`FALLBACK_COHERENCE_BLOCKS=128`), replacing the `floorDiv`+`hash64` cell-grid pick.
- **Polar ice_spikes cap** (`3f8e0366`) — LANDED. `keepPolarIceSpike` coherent cap (`POLAR_ICE_KEEP_THRESHOLD=0.45`) in both `sanitizeLandBiome` overloads; the baseline's forced forest/taiga→ice_spikes rewired to `snowy_plains`.
- **Equator overhaul: badlands gate + desert thinning** (`91ffa425` + `10e10ffd`) — LANDED, province-independent. `demoteEquatorialDryBiomes` at the end of both `applyFinalSavannaClimateClamp` overloads (badlands→savanna below the 10/18° ramp; partial desert thinning below 12°, keepFrac 0.40).
- **Warm-band province rebalance** (`8c73beab`) — **DEFERRED (Bucket 2)** on all three. It is built entirely on the `ProvinceAuthority`/`Province`/moisture subsystem that does not exist in the 1.3.0 line; porting it is Bucket-2-scale. Its equatorial Earth-analog goal is delivered here by the province-independent equator gates. (`FALLBACK_COHERENCE_BLOCKS=128` adopted; `TIER_COHERENCE_BLOCKS=160` has no analog in this selection path.)

The diff is `+152 / -7` to `LatitudeBiomes.java` on **all three** versions (plus the `gradle.properties` version bump and a binder report; on 1.21.11/1.21.1 also a `chmod +x gradlew` so checkout builds work).

**Porting note:** the 1.21.11 and 1.21.1 1.3.0 baselines have **byte-identical** `LatitudeBiomes.java`, so the transplant copied across verbatim. The 1.20.1 baseline differs by ~695 lines (older API) but every helper symbol + every anchor site the 1.4 logic needs is present with identical signatures, so the same transplant was re-applied surgically (5 hunks) to the identical `+152/-7` shape.

---
## Proof (map-based, headless atlas, NO chunk pregen — §5 targets all met)

Seeds `2533348776566713405` + `1199119911991199`, `--size=small --radius=7500 --step=16 --emitbiomeindex=true`.
Metrics via `tools/atlas/sublatitude_dry_wet.py` + `tmp/tropical-badlands-gate-ace6e326/perbiome_sublat.py`.
Because the worldgen algorithm is mapping-independent, the dry/wet per-band figures are identical across all three ports (each was re-run and verified independently, not assumed).

**Key before→after deltas (seed 2533348776566713405, % of land):**

| band | metric | baseline (pre-port) | 1.4-beta (all 3 ports) |
|---|---|---|---|
| 0–5° | badlands | (leak source, BoP/seed-dependent) | **0.00%** (structural 0 at deep equator) |
| 0–5° | desert | — | 5.45% (thinned, still present) |
| 0–5° | jungle | — | 82.4% (dominant, NOT mono — savanna 6.6% present) |
| 30–35° | desert (subtropical arid belt) | ~unchanged | **54.98%** (belt intact — gate off here) |
| polar | ice_spikes (whole-world) | **5.72%** | **2.91%** (under its ~6% accent cap) |
| whole-world | distinct biomes | 50 | 50 — **none vanished (Art X clean)** |

Seed `1199119911991199`: 0–5° badlands **0.00%** / desert **0.00%**; 30–35° desert 53.92% (belt intact); equator jungle 91.8% with savanna 5.1% (not a monoculture).

**Note:** the headless atlas is vanilla-biome-only (no BoP/Terralith/Promenade datapack stack), so equatorial jungle reads higher than the 26.1.2 BoP-stack figures. The §5 *qualitative* targets are what's validated (and all pass).

---
## The 1.20.1 refmap concern — RESOLVED (not a blocker for Stage 1)

The handoff flagged 1.20.1's `latitude-refmap.json` regime as the likely wall. Finding: the 1.20.1 build **does generate and embed `latitude-refmap.json`** (12 KB, 23 mixin classes mapped) and `globe.mixins.json` references it; `clean build` is green. Crucially, **Stage 1 touches only `LatitudeBiomes.java` (a plain worldgen class, not a mixin)**, so the mixin/refmap surface is unchanged from the shipped 1.3.0 baseline. The refmap wall is real but only bites when *new mixins* are added — which is Bucket 2, not this stage.

(1.21.11/1.21.1 emit no embedded refmap, matching their own shipped 1.3.0 release jars — the mixinextras annotation processor is disabled in those `build.gradle`s. Also pre-existing baseline behavior; this change is refmap-neutral there too.)

---
## Toolchain / build reproduction notes

- All three built with **local temurin-21** (`/usr/libexec/java_home -v 21`). JDK 17 is NOT installed on this machine; the 1.20.1 baseline `gradle.properties` already targets JDK 21 (not 17 as the handoff table guessed) and compiles+runs cleanly under it.
- 1.21.11's `gradle.properties` keeps the maintainer's **Windows** `org.gradle.java.home`; builds were done by passing `-Dorg.gradle.java.home=$(/usr/libexec/java_home -v 21)` on the command line (NOT by editing the committed file). 1.21.1 and 1.20.1 already point at the local macOS temurin-21.
- Headless atlas requires the watchdog off: set `max-tick-time=-1` in each worktree's `run-headless/server.properties` (local-only, uncommitted). With the default 60s it crashes the preview tick (Watchdog).
- Rebuild any version: `env -u JAVA_TOOL_OPTIONS JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew build -Dorg.gradle.java.home=$(/usr/libexec/java_home -v 21)` (the `-D` only needed for 1.21.11).

---
## EXACT NEXT ACTIONS for Maintainer (deferred per §7 — NOT done autonomously)

For each of the three branches (all pushed, all jars built + SHA-verified above):
1. Push the local `save/1.4.0-beta-worldgen-<mc>` annotated tag if you want it on origin (left local per guardrail).
2. Create the **GitHub pre-release** (Beta channel) per version, attaching the jar above. Title `1.4.0-beta.1+<mc>`.
3. **Modrinth** upload to the **Beta** channel (per `[[latitude-versioning-convention]]`, no "preview" string).
4. (Optional) real-GPU render check — N/A for Stage 1 (worldgen only).
5. Binder reports per version are committed on each branch under `docs/binder/1.4-beta-backport-<mc>-report-20260606.md`.

**Bucket 2 (later full-1.4 stage, do NOT ship in these betas):** warm-band province subsystem port; custom-biome source-wrap mixins + BoP/Terralith/Promenade tags; world-entry client. NOTE for 1.20.1: adding *new mixins* in Bucket 2 will require refmap re-verification.

---
## Per-version detail reports
- `docs/binder/1.4-beta-backport-1.21.11-report-20260606.md` (on `port/1.4.0-beta-1.21.11`)
- `docs/binder/1.4-beta-backport-1.21.1-report-20260606.md` (on `port/1.4.0-beta-1.21.1`)
- `docs/binder/1.4-beta-backport-1.20.1-report-20260606.md` (on `port/1.4.0-beta-1.20.1`)

---
---

# STAGE 2 — toward full 1.4 parity (custom-biome tags + 2b assessment)

**Run:** overnight 2026-06-06 (continued), autonomous (Opus, high reasoning), per `docs/porting/HANDOFF-1.4-beta-backport.md` §8.
**Outcome: 2a (custom-biome TAG admission) DONE + PROVEN absent-mod-safe on ALL THREE versions; bumped to `1.4.0-beta.2+<mc>`, branches pushed.** 2b (source-wrap + world-entry) assessed: source-wrap already present/working in the baselines; the only genuine 2b gap is the 1.21.1 world-entry render-gate, which is BLOCKED (render-unverifiable + refactored API) — documented, not forced. No guardrail violations (main = `4ccb9e9e…`, 26.1.2 = `691e54b4…` untouched; no tags pushed).

## Stage-2 TL;DR table

| MC | 2a tags (PROVEN absent-mod-safe) | jar `1.4.0-beta.2+<mc>` | SHA-256 | local save tag | 2b |
|---|---|---|---|---|---|
| **1.21.11** | 36 entries (BoP+Terralith+Promenade) | `latitude-1.4.0-beta.2+1.21.11.jar` | `5338041f3ac2a49efcb3c723469d69bfa42bc4dbb78cc6ec82939012d1e72ba4` | `save/1.4.0-beta2-custombiome-1.21.11` | source-wrap present; world-entry already in baseline |
| **1.21.1** | 94 entries (BoP+Terralith+NS+RU+Promenade) | `latitude-1.4.0-beta.2+1.21.1.jar` | `11984d982c4cce790f69aeaad228650b50354af9d04d7c2a742281c3bbe8b90b` | `save/1.4.0-beta2-custombiome-1.21.1` | source-wrap present; **world-entry render-gate BLOCKED** |
| **1.20.1** | 94 entries (BoP+Terralith+NS+RU+Promenade) | `latitude-1.4.0-beta.2+1.20.1.jar` | `db976c2cf6294170c7d7abe5f074bd70107fc45777e795c2dd38faec034fe1b2` | `save/1.4.0-beta2-custombiome-1.20.1` | source-wrap + world-entry both already in baseline |

All three branches **pushed** to `origin`. **No tags pushed** (`save/*` local-only). Jars at `…/Latitude-port-1.4.0-<mc>/build/libs/`.

## Three buckets per version

### PROVEN (2a — atlas-validated, absent-mod-safe + Art X)
Custom-biome `lat_*` tag entries added via `scripts/apply_custom_biome_tags.py` (canonical repo), all `required:false`, climate-placed by real-world analog:
- **1.21.11** — 36 entries, BoP+Terralith+Promenade (mirrors proven 26.1.2 set `0965687c`). Atlas: 50 distinct vanilla biomes, 0 non-`minecraft:`, none vanished.
- **1.21.1** — 94 entries, BoP+Terralith+Nature's Spirit+Regions Unexplored+Promenade. Atlas: 48 distinct vanilla biomes, Art X clean.
- **1.20.1** — 94 entries (same set + placement as 1.21.1; tags-only so refmap untouched). Atlas: 49 distinct vanilla biomes, Art X clean.

Proof method (all three): vanilla headless atlas (`runBiomePreview`, NO mods, NO pregen), seed `2533348776566713405`, small/R7500/step16. The datapack loads with every `required:false` custom entry present and worldgen is generation-neutral (no vanilla biome added/removed) — i.e. the tags are **absent-mod-safe** (the only configuration verifiable on this machine). Tag IDs are real: BoP/Terralith/Promenade verified against the 26.1.2 jars' `worldgen/biome/*.json`; Nature's Spirit from the mod wiki (snake_case registry convention); Regions Unexplored from the published v0.5.5 biome list. The functional admission chain (`lat_*` tags → `LatitudeBiomes` band selection → existing source-wrap) is complete at the worldgen level.

### NEEDS-PEETSA (tags-need-jar; render-unverifiable)
- **TAGS-ADDED-NEEDS-JAR-VALIDATION (all three):** the "biome appears in its tagged band / controlled share / coherent regions" proof needs the provider mods present at **runtime**. **The only biome-mod jars on this machine target MC 26.1.2** (BoP `depends.minecraft=26.1.2`, Promenade `~26.1-`, loader ≥0.18) and will not load on the 1.21.11 / 1.21.1 / 1.20.1 runtimes — so in-atlas mod-present validation could not be run for any target. Action for Maintainer: drop version-matched BoP/Terralith/Nature's Spirit/Regions Unexplored/Promenade jars in each worktree's `run-headless/mods` and re-run the atlas (mirror `docs/binder/custom-biome-sourcewrap-promenade-report-20260606.md`) to confirm placement + share. Note: **Promenade has no published 1.21.1 build** (its range is 1.21.4–1.21.11 and 1.20–1.20.2), so its `required:false` entries stay inert on 1.21.1 (harmless); it does have 1.20.1 + 1.21.11 builds.
- **NEEDS-PEETSA-RENDER-CHECK (1.20.1):** the world-entry render-gate overlay + latitude early-spawn already ship in the 1.20.1 baseline (`LevelLoadingScreenLatitudeOverlayMixin` + `MinecraftServerInitialSpawnMixin`, both registered). They compile and are wired, but their render-ready behavior is **not headlessly verifiable** — Maintainer should do a real-GPU entry check before promoting to plain `1.4.0`.

### BLOCKED (didn't map / not forced)
- **2b warm-band province subsystem (all three):** unchanged from Stage 1 — built on `ProvinceAuthority`/`Province`/moisture, which the 1.3.0 line lacks; Bucket-2-scale. Out of Stage-2 scope.
- **2b world-entry render-gate on 1.21.1 — BLOCKED (anti-spiral, 1 deep attempt):** 1.21.1 is the only version *missing* `MinecraftServerInitialSpawnMixin` + `LevelLoadingScreenLatitudeOverlayMixin`. Porting them cleanly needs two things that don't map: (1) `GlobeMod.trySetInitialLatitudeSpawn` does not exist in the 1.21.1 source (the spawn helper the mixin calls), and (2) `LevelLoadingScreen` moved package/refactored its API between 1.20.1 (`net.minecraft.client.gui.screen.LevelLoadingScreen`, `WorldGenerationProgressTracker`) and 1.21.1 (`net.minecraft.client.gui.screen.world.LevelLoadingScreen`), so the 1.20.1 Yarn overlay mixin's `render`/`getPercentage`/`removed` injection points do not transfer. Per §8 (NEVER certify render behavior; do not force what doesn't map; do not break a working baseline) this was left BLOCKED rather than shipping an unverifiable render mixin into the 1.21.1 baseline, which already has a working entry path (`GlobeWarningOverlay` + `DownloadingTerrainScreenFirstLoadMessageMixin`). 2a custom-biome admission does **not** depend on this; it is purely the entry-UX refinement.

### 2b source-wrap — already present (not a gap)
Contrary to the handoff's Bucket-2 expectation, the source-wrap mixins (`ChunkGeneratorBiomeSourceMixin`, `ChunkGeneratorPopulateBiomesMixin`, `BiomeSourceAccessor`, `LatitudeBiomeSource`) **already exist, are registered in `globe.mixins.json`, and compile** in BOTH the 1.21.1 and 1.20.1 baselines (they shipped in the 1.3.0 line with correct Yarn descriptors — the 26.1.2 commit `d133f63b` was a *Mojang-mappings descriptor fix* that the Yarn ports never needed). So no source-wrap port was required; the 2a tags are read through the existing wrap.

## Toolchain / reproduction (Stage 2)
- Same as Stage 1: local temurin-21; 1.21.11 needs `-Dorg.gradle.java.home=$(/usr/libexec/java_home -v 21)` (its committed `org.gradle.java.home` is the maintainer's Windows path, left untouched); `max-tick-time=-1` in `run-headless/server.properties` for the atlas (local-only, restored after).
- Re-apply tags from scratch (idempotent): `python3 scripts/apply_custom_biome_tags.py <worktree>/src/main/resources/data/globe/tags/worldgen/biome <modset>` where modset = `bop,terralith,promenade` (1.21.11) or `bop,terralith,promenade,natures_spirit,regions_unexplored` (1.21.1 / 1.20.1).

## EXACT NEXT ACTIONS for Maintainer (Stage 2)
1. **Validate 2a with real jars:** per version, place version-matched BoP/Terralith/(NS/RU on 1.21.1+1.20.1)/Promenade jars in `run-headless/mods` and re-run the atlas; confirm each custom biome lands in its tagged band with controlled share + coherent regions, Art X clean. Then the 2a placement graduates from `NEEDS-JAR-VALIDATION` to fully proven.
2. **1.20.1 world-entry:** real-GPU entry check (render-gate timing + latitude spawn) before any plain-`1.4.0` promotion.
3. **1.21.1 world-entry render-gate:** decide whether to invest in the from-scratch 1.21.1 `LevelLoadingScreen` (new package/API) port + a `GlobeMod.trySetInitialLatitudeSpawn` for 1.21.1, or ship 1.21.1 beta.2 with its existing baseline entry path. (Custom-biome admission is unaffected either way.)
4. Publishing (all three): push `save/1.4.0-beta2-custombiome-<mc>` tags if desired (local-only now), GitHub pre-release (Beta), Modrinth Beta upload — all reserved for you. The plain `1.4.0+<mc>` Release stays gated on your render verification.

---
---

# STAGE 3 — broaden custom-biome compatibility to MORE popular mods, ALL versions incl. 26.1.2

**Run:** overnight 2026-06-06 (continued), autonomous (Opus, high reasoning), per `docs/porting/HANDOFF-1.4-beta-backport.md` §9 (Maintainer: "do it for 26.1.2 too!!").
**Outcome: custom-biome TAG admission broadened to FOUR more popular biome mods (BYG, Terrestria, Wilder Wild, Traverse) on ALL FOUR lines — the three port branches PLUS a NEW 26.1.2 expansion branch.** Every line: tags applied, version bumped, vanilla headless atlas proves absent-mod-safe + generation-neutral + Art X clean. On 26.1.2, an additional mod-PRESENT atlas (real BoP/Terralith/Promenade jars) proves the full admission chain end-to-end. No guardrail violations (`main` = `4ccb9e9e`, released `feat/1.3.1-cohesive-horizons-26.1.2` = `691e54b4` both untouched; no tags pushed).

## Stage-3 TL;DR table

| MC / line | branch (pushed) | mod_version | new entries | total custom | Stage-3 proof |
|---|---|---|---|---|---|
| **26.1.2** | `feat/custom-biome-expansion-26.1.2` (NEW, off `691e54b4`) | `1.4.1-beta.1+26.1.2` | 101 | 139 | vanilla atlas (gen-neutral, Art X) **+ mod-present atlas: 77 biomes each in correct band, real BoP/Terralith/Promenade** |
| **1.21.11** | `port/1.4.0-beta-1.21.11` | `1.4.0-beta.3+1.21.11` | 101 | 137 | vanilla atlas: 50 vanilla biomes, gen-neutral, Art X clean |
| **1.21.1** | `port/1.4.0-beta-1.21.1` | `1.4.0-beta.3+1.21.1` | 101 | 195 | vanilla atlas: 48 vanilla biomes, gen-neutral, Art X clean |
| **1.20.1** | `port/1.4.0-beta-1.20.1` | `1.4.0-beta.3+1.20.1` | 101 | 195 | vanilla atlas (clean run): 49 vanilla biomes, gen-neutral, Art X clean |

All four branches **pushed**. Local-only `save/*` tags created, **not pushed**: `save/custom-biome-expansion-26.1.2`, `save/1.4.0-beta3-custombiome-{1.21.11,1.21.1,1.20.1}`.

## Mods added (the §9 set) — real registry IDs, climate-placed

The same 101 `required:false` `lat_*` tag entries were applied to every line via `scripts/apply_custom_biome_tags.py` (extended with four new modsets: `byg`, `terrestria`, `wilder_wild`, `traverse`):

| Mod | namespace | entries | ID source (verified) |
|---|---|---|---|
| **Oh The Biomes You'll Go (BYG)** | `byg` | 51 | `Potion-Studios/BYG` `BYGBiomes.java` — all 51 overworld biomes (+ beaches excluded); nether/end excluded |
| **Terrestria** | `terrestria` | 20 | `TerraformersMC/Terrestria` `TerrestriaBiomes.java` |
| **Wilder Wild** | `wilderwild` | 23 | `WilderWild-4.2.9-mc26.1.jar` — IDs **and** temperature/downfall read straight from the jar's `worldgen/biome/*.json` (the most precise placement of the four); caves/coast/river specials excluded |
| **Traverse** | `traverse` | 7 | `TerraformersMC/Traverse` `TraverseBiomes.java` |

Climate placement is by temperature/humidity/name analog into the existing band semantics (subpolar=boreal, temperate=mid-lat forest, `_mountain`=alpine, `_accent`=meadow/floral, arid=hot desert, trans_arid_tropics=savanna, tropics=jungle/rainforest). All `required:false` (absent-mod-safe). Already-admitted set (BoP/Terralith/Promenade ± NS/RU) re-verified consistent per line (the re-apply was a clean idempotent no-op).

## PROVEN (atlas-validated)

- **All four lines — absent-mod-safe + generation-neutral + Art X clean (vanilla headless atlas, seed `2533348776566713405`, small/R7500/step16):** the datapack loads with every one of the 101 new `required:false` entries present, and worldgen is unchanged — **0 non-`minecraft:` biomes appear, the per-line vanilla biome inventory matches its Stage-2 baseline (none vanished), the equator stays jungle-dominant but not a monoculture.** This is the same certification Stage 2 used.
- **26.1.2 — full admission chain PROVEN end-to-end (mod-present atlas, real provider jars):** with BoP 26.1.2.0.2 + TerraBlender + GlitchCore, Terralith 2.6.2, and Promenade 5.5.0 loaded as actual mods + datapacks, the atlas shows **77 distinct biomes (45 vanilla + 20 BoP + 9 Terralith + 3 Promenade), each landing in its correctly-tagged `lat_*` band** (e.g. `biomesoplenty:tundra`→subpolar, `biomesoplenty:tropics`→tropical, `terralith:siberian_taiga`→subpolar, `promenade:glacarian_taiga`→subpolar, `promenade:blush_sakura_grove`→temperate), with controlled share and Art X clean (vanilla biomes all still present, no monoculture). This proves `lat_*` tag → `LatitudeBiomes` band-select → source-wrap works for resolvable mods; the four NEW mods reuse the identical chain and entry shape.

## NEEDS-JAR-VALIDATION (placement is high-confidence; only the runtime in-band proof is outstanding)

- **BYG, Terrestria, Traverse — all four lines:** no resolvable version-matched jar on this machine (the only biome-mod jars present target MC 26.1.2; BYG/Terrestria/Traverse had no 26.1.2 Fabric build found in this pass, and none of the three has a jar here for 1.21.11 / 1.21.1 / 1.20.1 either). Registry IDs are real (from each mod's source). They do not alter vanilla worldgen (proven gen-neutral).
- **Wilder Wild — all four lines (incl. 26.1.2):** a version-matched **26.1 jar IS present** (`WilderWild-4.2.9-mc26.1.jar`) and its 23 entries are placed from the jar's own temperature/downfall, but the jar `depends` on **FrozenLib** (`>=2.4.4`), which is not on this machine — Fabric won't load WW without it, so in-atlas mod-present placement could not be exercised (anti-spiral: did not chase the external dep chain overnight).
- **Action for Maintainer (per line):** drop version-matched BYG / Terrestria / Wilder Wild (**+ FrozenLib + Fabric API**) / Traverse jars into the worktree's `run/mods`, then `./gradlew runBiomePreview -Platdev.syncPreviewMods=true --args="--latdevBiomePng --seed=94717 --size=small --radius=7500 --step=16 --emitbiomeindex=true --out=/tmp/<dir>"` and inspect `world_biome_inventory.json` placement_band per custom biome (mirror the 26.1.2 mod-present proof above). Then these graduate from NEEDS-JAR-VALIDATION to fully proven.

## NEEDS-PEETSA-PLACEMENT-REVIEW (genuinely ambiguous bands — best guess placed AND flagged)

Marked inline in `scripts/apply_custom_biome_tags.py` with `# REVIEW` and reproduced here:
- `byg:jacaranda_forest` — jacaranda is subtropical IRL; placed `temperate_secondary` (flowering analog).
- `byg:crag_gardens` — rocky garden; placed `temperate_mountain` (alpine guess).
- `byg:temperate_rainforest` — wet; placed `tropics_secondary` (could be temperate-humid).
- `byg:lush_stacks` — lush wet stacks; placed `tropics_secondary` (guess).
- `byg:coniferous_forest` — name implies cold, but BYG registers it warm/humid; left in `tropics_secondary` — verify climate.
- `terrestria:oasis` — desert water-feature; placed `tropics_secondary` (humid) — could be `arid_accent`.
- `terrestria:volcanic_island` — placed `tropics_secondary` (tropical-island guess).
- `traverse:lush_swamp` — warm wetland; placed `tropics_secondary` (could be temperate).

UNADMITTED on purpose (no confident land band; need a coast gate): `byg:rainbow_beach`, `byg:basalt_barrera`, `byg:dacite_shore`.

## EXACT NEXT ACTIONS for Maintainer (Stage 3)
1. **Jar-validate the four new mods** per line as above (esp. add FrozenLib so Wilder Wild loads; WW + 26.1.2 is the lowest-effort first win since the WW jar is already here).
2. **Review the 8 `NEEDS-PEETSA-PLACEMENT-REVIEW` bands** (BYG dominates the ambiguity — large pack); re-place any you disagree with by editing the modset in `scripts/apply_custom_biome_tags.py` and re-running it (idempotent).
3. **Publishing/merge — all deferred to you:** push `save/*` tags if desired (local-only now); decide whether `feat/custom-biome-expansion-26.1.2` merges back to the released line or stays a feature branch; GitHub pre-release / Modrinth Beta for the bumped port betas. No tags/releases/Modrinth were touched autonomously.

## Per-line detail reports
- `docs/binder/custom-biome-expansion-26.1.2-report-20260606.md` (on `feat/custom-biome-expansion-26.1.2`) + binder evidence row `20260606-custom-biome-expansion-26.1.2`.
- Stage-3 section appended to each port branch's `docs/binder/1.4-beta-backport-<mc>-report-20260606.md`.

---
---

# STAGE 4 (FINAL) — CliffTree custom-biome admission, ALL FOUR lines

**Run:** overnight 2026-06-06 (continued), autonomous (Opus, high reasoning), per `docs/porting/HANDOFF-1.4-beta-backport.md` §9 (Maintainer: CliffTree specifically requested).
**Outcome: CliffTree (Modrinth slug `clifftree`) custom-biome TAG admission landed on ALL FOUR lines. CliffTree is a vanilla-styled worldgen DATAPACK, so — unlike the Stage-3 jar-only mods — it was DATAPACK-PRESENT atlas-validated for real on three of four lines (1.21.11, 1.20.1, 26.1.2 all PROVEN with the version-matched CliffTree datapack actually loaded, biomes appearing in their tagged bands). 1.21.1 is gen-neutral-proven but its CliffTree backport datapack won't load standalone (needs CliffTree's companion mod jar) → NEEDS-DATAPACK-VALIDATION.** No guardrail violations (`main` = `4ccb9e9e`, released `feat/1.3.1-cohesive-horizons-26.1.2` = `691e54b4` both untouched; no tags pushed).

## Stage-4 TL;DR table

| MC / line | branch (pushed) | mod_version | jar | SHA-256 | Stage-4 proof |
|---|---|---|---|---|---|
| **26.1.2** | `feat/custom-biome-expansion-26.1.2` | `1.4.1-beta.2+26.1.2` | `latitude-1.4.1-beta.2+26.1.2.jar` | `500c39264a4c7d63b1bc1693be697a1f19cd937aee336fe89d007ff70c0b89de` | **PROVEN** datapack-present (CliffTree 3.2.1) alongside live BoP+Terralith+Promenade |
| **1.21.11** | `port/1.4.0-beta-1.21.11` | `1.4.0-beta.4+1.21.11` | `latitude-1.4.0-beta.4+1.21.11.jar` | `55155f0af3dc439960572cb59dcca4bb04d624632059f9cccf466b74d952360f` | **PROVEN** datapack-present (CliffTree 3.1.5 Mounts-of-Mayhem) |
| **1.20.1** | `port/1.4.0-beta-1.20.1` | `1.4.0-beta.4+1.20.1` | `latitude-1.4.0-beta.4+1.20.1.jar` | `32cbaa06de76f43f0d74206ad84bb9264a30517b1e0c23922a43c847af989c06` | **PROVEN** datapack-present (Clifftree 1.7.2 backport) |
| **1.21.1** | `port/1.4.0-beta-1.21.1` | `1.4.0-beta.4+1.21.1` | `latitude-1.4.0-beta.4+1.21.1.jar` | `de7e8eb8e63234fe926e4eed38fbac44eb92b40060953593f3988857bc609603` | vanilla gen-neutral PROVEN; **NEEDS-DATAPACK-VALIDATION** (see below) |

All four branches **pushed**. Local-only `save/*` tags created, **NOT pushed**: `save/clifftree-expansion-26.1.2`, `save/1.4.0-beta4-clifftree-{1.21.11,1.21.1,1.20.1}`. Jars at `…/Latitude-{port-1.4.0-<mc> | custom-biome-expansion-26.1.2}/build/libs/`.

## What was added — real IDs from the version-matched CliffTree datapacks

The same **8** `required:false` `clifftree:*` `lat_*` entries were applied to every line via `scripts/apply_custom_biome_tags.py` (extended with a new `clifftree` modset). IDs + temperature/downfall were read straight from each line's version-matched CliffTree datapack zip (`data/clifftree/worldgen/biome/*.json`) downloaded from Modrinth (`https://api.modrinth.com/v2/project/clifftree/version`):
- 26.1.2 → CliffTree 3.2.1 (`26.1`); 1.21.11 → 3.1.5 Mounts-of-Mayhem; 1.21.1 → 3.1.5 Backport; 1.20.1 → Clifftree 1.7.2 Backport.

Only **surface-land** biomes get a latitude band. Caves / oceans / rivers / shores+beaches+cliffs / sky-dimension biomes are **excluded** (they need cave/coast/river/ocean/dimension gates, not latitude bands — same discipline as Wilder Wild's caves/coast/river in Stage 3).

| clifftree biome | T / D (from JSON) | band tag | analog |
|---|---|---|---|
| `glacier_valley` | 0.0 / 0.5 | `lat_polar_secondary` | snowy ice/glacier |
| `snowy_old_growth_taiga` | 0.0 / 0.4 | `lat_subpolar_primary` | snowy boreal taiga |
| `tundra` | 0.25 / 0.5 | `lat_subpolar_secondary` | cold tundra |
| `bog` | 0.25 / 0.8 | `lat_subpolar_secondary` | cold wet boreal wetland |
| `sparse_forest` | 0.7 / 0.8 | `lat_temperate_secondary` | temperate humid forest |
| `shrubland` | 2.0 / 0.0 | `lat_arid_accent` | hot dry shrub |
| `oasis` | 2.0 / 0.0 | `lat_arid_accent` | hot desert water-feature — **REVIEW** |
| `coniferous_badlands` | 2.0 / 0.0 | `lat_arid_accent` | name says "coniferous" (cold) but JSON is HOT+DRY → placed by real climate — **REVIEW** |

(1.20.1's older datapack omits `coniferous_badlands`; its `required:false` entry is harmless there.) Excluded on purpose: `caves, warm_caves, lukewarm_caves, cold_caves, frozen_caves, mushroom_caves, dirt_caves, pale_grotto, inferno` (caves); `stone_ocean, kelp_forest` (ocean); `warm_river, cold_river, tropical_river` (rivers); `diorite_shore, snowy_diorite_shore, granite_shore, glacier_cliff, desert_cliff, gravelly_beach, tropical_beach, temperate_beach` (shores/beaches/cliffs); `sky/*` (sky dimension, 26.1 only).

## PROVEN (datapack-present atlas — the real thing, not absent-mod-safe-only)

Seed `2533348776566713405`, small/R7500/step16, vanilla-baseline vs CliffTree-datapack-dropped-into-`world/datapacks`. In all three the CliffTree datapack loaded ("Found new data pack file/clifftree, loading it automatically") and **every admitted `clifftree:*` biome appeared, each in its tagged `lat_*` band**, with a controlled whole-world share and Art X clean:

- **1.21.11** — 8/8 clifftree biomes present; share **5.97%**; `glacier_valley`→Polar 90N, `snowy_old_growth_taiga`→Polar 74N, `tundra`→Subpolar 64N, `bog`→Subpolar 63N, `sparse_forest`→Temperate 49N, arid trio at the warm/Temperate-38N edge. 50 vanilla biomes stable (Art X). 0 unexpected non-`minecraft:`/`clifftree:` biomes.
- **1.20.1** — 7/7 present (datapack omits `coniferous_badlands`); share **5.98%**; identical band placement. 49 vanilla biomes stable (Art X).
- **26.1.2** — 8/8 present **alongside live BoP+Terralith+Promenade** (richest env: their Stage-2/3 biomes also placed correctly); share **5.98%**; no monoculture (top whole-world biome only 9.1%). Bands as above.

**Art X note (all three):** a within-badlands-family first-seen reshuffle (`wooded_badlands`↔`badlands` on the ports) shows up at the discovery step; the family stays present and the distinct-biome count is stable, so this is sampling noise from CliffTree's vanilla surface touches, not a vanish. On **26.1.2** there is one documented caveat: `minecraft:beach` drops out of the first-seen inventory because CliffTree's own coast/shore overhaul (`is_cliff` + `tropical_beach`/`granite_shore`/etc., which we deliberately did NOT lat-band) takes over coast cells — this is **CliffTree's intended behavior, not a Latitude regression**. Distribution is healthy (no monoculture), so it does not trip Art X.

## NEEDS-DATAPACK-VALIDATION (anti-spiral, 2 attempts)

- **1.21.1** — vanilla atlas is gen-neutral PROVEN (48 vanilla biomes, 0 non-`minecraft:`). But the CliffTree **1.21.1 backport datapack will not load standalone** in the headless server: both the 3.1.5 and 3.1.4 backports reference newer-MC worldgen features (`minecraft:fallen_tree`, `minecraft:leaf_litter`, `minecraft:place_on_ground`, `pale_garden_*`) that don't exist on a pure 1.21.1 runtime — these backports expect CliffTree's **companion mod jar** to register those features via code. Datapack load aborts ("Failed to load datapacks, can't proceed"). This is a CliffTree-backport-vs-runtime gap, **not** a Latitude/tags problem. Confidence is high regardless: the 8 `clifftree:*` IDs are identical across all lines and the **1.21.11 line — same 8 IDs, same tag entries — is PROVEN datapack-present**. Action for Maintainer: drop CliffTree's 1.21.1 **mod jar** (`clifftree-3.1.5-1.21.1_backport.jar`) into `run-headless/mods` (+ Fabric API) so the features register, then re-run the atlas.

## NEEDS-PEETSA-PLACEMENT-REVIEW (best guess placed AND flagged; inline `# REVIEW` in the script)

- `clifftree:oasis` — T2.0/D0.0 desert water-feature; placed `lat_arid_accent` (hot-desert), could be a tropics-humid pocket.
- `clifftree:coniferous_badlands` — name implies cold conifer, but the JSON registers it HOT+DRY (T2.0/D0.0, no precipitation, in CliffTree's `dry_badlands` family) → placed `lat_arid_accent` by its real climate. Flag the name/climate mismatch.

## EXACT NEXT ACTIONS for Maintainer (Stage 4 — the chain is now COMPLETE)
1. **1.21.1 datapack-present validation:** add the CliffTree 1.21.1 mod jar (not the standalone datapack) + Fabric API to `run-headless/mods` and re-run the atlas; then 1.21.1 graduates from NEEDS-DATAPACK-VALIDATION to PROVEN (the IDs/tags are identical to the PROVEN 1.21.11 line).
2. **Review the 2 `NEEDS-PEETSA-PLACEMENT-REVIEW` bands** (`oasis`, `coniferous_badlands`); re-place by editing the `clifftree` modset and re-running the (idempotent) script if you disagree.
3. **Publishing/merge — all deferred to you:** push `save/*` tags if desired (local-only now); GitHub pre-release (Beta) / Modrinth Beta for each bumped jar above; decide the 26.1.2-expansion-branch merge. No tags/releases/Modrinth were touched autonomously.
